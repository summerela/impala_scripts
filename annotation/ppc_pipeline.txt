## predicted protien change (ppc) pipeline

if length(ref) == length(alt):
    var_type = subtitution
    if effect_type IN ('codings_sequence_variant', 'chromosome', 
    'missense_variant', 'initator_codon_variant', 'stop_retained_variant', 'transcript')
        consequence = non-synonymous ppc
    else:
        consequence = synonymous ppc

elif length of ref > len of alt:
    var_type = insertion
    if there is a frameshift ('frameshift_variant'):
        consequence = frameshift, non-synonymous ppc
    else there is no frameshift:
         if there is an aa change ('disruptive_inframe_insertion'):
             consequence = non-synonymous pcc
         else:
             synonymous pcc

elif length of ref < lenght of alt:
    var_type = deletion
    if there is a frameshift ('frameshift_variant'):
        consequence = frameshift, non-synonymous pcc
    else there is no frameshift:
        if there is aa change ('disruptive_inframe_deletion):
             consequence = non-synonymous ppc
        else:
            consequence = synonymous ppc

if variant in 5' splice site, 3' splice site, premature stop, loss of stop
    ('splice_acceptor_variant', 'splice_donor_variant', 'stop_lost', 'start_lost', 'stop_gained', '5_prime_UTR_premature start_codon_gain_variant') :
    consequence = non-synonymous ppc 



unsure: 

downstream_gene_variant
exon_variant
gene_variant
miRNA





